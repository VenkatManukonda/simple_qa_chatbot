# Simple Q&A Chatbot

This project implements a simple terminal-based chatbot using Python.

## Features

- Simple AI-like responses
- Runs in terminal (no web interface)
- Easy to expand with more responses
- Demonstrates basic conversational AI concepts

## How to Run

1. Install Python 3.x
2. Create and activate a virtual environment (optional but recommended)
3. Install dependencies (if using Django or ChatterBot in future):
