# Simple Q&A Chatbot

This project implements a simple terminal-based chatbot using Python.

## How to Run
1. Install Python
2. Install dependencies:
   pip install -r requirements.txt
3. Run the chatbot:
   python chatbot.py

The chatbot interacts with the user through the terminal and responds to common greetings and questions.
